name: Deploy SDK docs

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "go/docs/**"
      - "typescript/docs/**"
      - "typescript/mkdocs.yml"
      - "python/docs/**"
      - "python/mkdocs.yml"
      - ".github/workflows/docs.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - name: Build Go docs
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./go/docs
          destination: ./_site/go
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install mkdocs-material
      - run: cd typescript && mkdocs build --strict --site-dir ../_site/typescript
      - run: cd python && mkdocs build --strict --site-dir ../_site/python
      - run: cp docs/index.html _site/index.html
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
